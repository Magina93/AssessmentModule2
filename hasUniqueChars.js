// Write your code below
let myString = "Monday";

function hasUniqueChars(string){
	for (let i=0; i < string.length -1; i++) { 
		 return 'true';
	} 
	return 'false';
}

console.log(hasUniqueChars(myString));